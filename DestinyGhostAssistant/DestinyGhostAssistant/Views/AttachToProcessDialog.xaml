<Window x:Class="DestinyGhostAssistant.Views.AttachToProcessDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DestinyGhostAssistant.Views"
        mc:Ignorable="d"
        Title="Attach to Process" Height="450" Width="600"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>    <!-- Prompt -->
            <RowDefinition Height="*"/>       <!-- ListBox -->
            <RowDefinition Height="Auto"/>    <!-- Buttons -->
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Margin="0,0,0,10">Select a process to attach to:</TextBlock>

        <ListBox Grid.Row="1" x:Name="ProcessListBox" Margin="0,0,0,15"
                 ItemsSource="{Binding Processes}"
                 DisplayMemberPath="DisplayName"
                 SelectionChanged="ProcessListBox_SelectionChanged"
                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                 ScrollViewer.HorizontalScrollBarVisibility="Auto">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="OkButton" Content="OK" Width="80" Height="25" IsDefault="True"
                    Click="OkButton_Click" Margin="0,0,10,0" IsEnabled="False"/>
            <Button x:Name="CancelButton" Content="Cancel" Width="80" Height="25" IsCancel="True"
                    Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
